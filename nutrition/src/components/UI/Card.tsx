import { CardComponentProps } from "..";

/**
 *
 *
 * @param {*} {children} chil
 * @return {*}
 */
const Card: React.FC<CardComponentProps> = ({ children }) => {
  return <main className="p-[20px]"></main>;
};

export default Card;
